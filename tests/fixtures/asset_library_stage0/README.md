# Stage-0 migration fixture

This directory is reserved for synthetic legacy manifest fixtures used by the
asset-library migration tests. Fixtures must never contain production media or
private brand data. Tests may create small placeholder files beside copied
manifests when checksum and missing-file behavior is under test.
