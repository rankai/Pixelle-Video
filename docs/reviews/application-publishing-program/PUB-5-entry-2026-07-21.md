# E2E-DOUYIN / PUB-5 Entry（2026-07-21）

状态：`entry_passed_with_boundary`；PG-J 已 `passed_with_boundary`，Entry 已经独立复审通过，尚未进入 PUB-5 业务实现或真实平台动作。

## Entry 目标

冻结“真实成片 → ArtifactVersion → PublishPackage → 真实抖音账号 → PublishRun → 编辑器 `waiting_for_human`”的唯一端到端验收边界。自动化不得点击最终发布；真实扫码、第三方授权、上传、字段变更和挑战必须在到达暂停点时通知用户并等待确认。

## 允许范围

- 读取现有 PUB-3 adapter、PUB-4 publish center、PG-G live smoke 和 PG-J closure 证据；
- 生产 session/ArtifactVersion/PublishPackage/PublishRun 的契约与证据 schema；
- 真实账号 profile 隔离、幂等/hash、重启去重、waiting_for_human 和 FinalActionGuard 的失败测试；
- 一次性、可暂停的抖音 headful E2E 运行计划与脱敏证据模板。

## 禁止范围

- 不在 Entry 阶段打开抖音、不扫码、不授权、不上传、不修改字段、不创建真实 PublishRun、不点击最终发布；
- 不用本地 fixture 冒充真实生产成片或云端 RunningHub 结果；provider 余额/网络/登录阻塞必须原样登记；
- 不改变平台 release state，不扩大到快手、视频号或小红书，不处理管理员/RBAC/套餐/支付。

## 退出条件

- contract/fixture 测试通过并经独立六维 Entry 复审；
- 冻结真实运行的输入媒体、账号/profile、package fingerprint、run idempotency、字段/封面回读和停止点；
- 明确所有真实外部动作暂停点，并准备最终发布 click_count=0 的证据字段；
- Entry 通过后才允许一次有界真实 E2E；真实扫码/授权/上传/最终发布仍需按暂停协议逐次通知用户。

契约：[`pub-5-e2e-douyin-entry.contract.json`](../../contracts/publishing/pub-5-e2e-douyin-entry.contract.json)

Fixture：[`pub-5-e2e-douyin-entry-fixtures.json`](../../contracts/publishing/fixtures/pub-5-e2e-douyin-entry-fixtures.json)

独立复审：[`PUB-5-entry-review-2026-07-21.md`](PUB-5-entry-review-2026-07-21.md)，结论 `entry_passed_with_boundary`、P0/P1=0。
