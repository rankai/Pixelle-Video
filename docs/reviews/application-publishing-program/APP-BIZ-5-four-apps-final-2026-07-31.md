# 应用中心四应用整体终审记录

日期：2026-07-31

## 结论

四应用整体独立严格只读复验：**PASS**。

覆盖应用：门店营销文案、爆款标题、抖音图文、数字人视频/口播。

## 六维验证依据

- 需求完整性：四个应用均具备真实入口、项目/素材输入、AppRun、Artifact、结果历史以及复制/预览/下载/发布交接。
- 逻辑正确性：统一复用 `AppCenterRepository`、`ProjectContextResolver`、`AppRun`、`Artifact` 和结果历史投影；没有新增第二套运行或产物权威。
- 边界情况：覆盖空输入、版本漂移、归档素材、跨项目引用、幂等重试、失败恢复、数字人声音绑定和媒体文件安全校验。
- 代码质量：生产代码、契约和测试 Ruff 通过，职责边界清晰，`git diff --check` 通过。
- 测试覆盖：后端相关回归 `586 passed, 12 warnings`；桌面端最终全量 `153 passed`；56 份应用中心契约 JSON、Python compileall 均通过；production build 通过。
- 实际运行：UI/API HTTP 200；`/api/apps` 返回四个应用且均为 enabled/ready；数字人两种模式完成受控媒体产物闭环；真实 MP4 为 H.264 + AAC、1080×1920、5.291667 秒。

## 复验备注

桌面端首次全量复验出现一次单测超时；失败用例单独运行、数字人测试文件 25/25 以及随后全量复跑 153/153 均通过，判定为测试环境并发抖动而非稳定产品问题。

开发服务器未加载 `.env.production` 时会回退旧工作台；生产配置已开启应用中心 flags，production build 和 production preview 路由验证通过。

真实数字人证据使用已有真实媒体通过受信路径导入、注册、审核和完成，未执行付费 provider 调用或外部平台发布。
