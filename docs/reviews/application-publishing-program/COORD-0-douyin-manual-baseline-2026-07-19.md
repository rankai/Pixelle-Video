# COORD-0 抖音授权/上传基线记录

日期：2026-07-19；执行者：主线程；平台：抖音创作者中心；结果：`blocked_external_manual`

## 目的

在不触发最终发布的前提下，验证上传入口可见性和测试媒体选择流程；未出现登录提示但不证明认证已通过，作为 PG-A 的外部 live/manual baseline。

## 预检

- Chrome 前台标签页为抖音创作者中心。
- 当前 URL：`creator.douyin.com/creator-micro/content/upload`。
- 页面出现“发布视频”“上传视频”“选择文件”，未出现二维码或登录提示；这只证明入口可见和会话存在性部分可见，不宣称账号认证已通过。
- 未记录或输出账号标识、Cookie、密码、验证码等敏感信息。
- 测试媒体：`data/video_assets/overlay/9f021312c89c.mp4`（本地既有 fixture）。

## 单次执行记录

1. 打开“选择文件”原生文件选择器。
2. 通过“前往文件夹”进入 `data/video_assets/overlay`。
3. 选中 `9f021312c89c.mp4`；原生选择器确认文件处于选中状态。
4. 选择器“打开”按钮持续为 disabled；Return、空格、双击、已暴露的“打开”辅助动作均未关闭选择器或提交文件。
5. 使用 Escape 取消选择器，回到上传页。

## 结果与边界

- 上传页仍显示“选择文件：未选择任何文件”。
- 未向抖音上传媒体；未填写标题、描述、话题或封面；未进入预审/草稿保存；未触发“发布/确认发布/立即发布”。
- 本次没有进行无依据重复尝试；失败原因已收敛为当前 Computer Use 对该原生选择器的提交动作不可用，或平台/文件类型过滤状态未能通过辅助接口确认。
- 因此只能确认上传入口可见且未出现登录提示，不能声称认证或上传链路通过；PG-A 继续保持 `in_progress`，外部条件保持 `blocked_external_manual`。
- 本次失败记录未生成截图/录像哈希或精确计时；作为“选择器无法提交”的阻塞证据足够。下一次用户手动接管必须补开始/结束时间、意图动作数、picker/upload 状态截图哈希。

## 恢复条件

用户在当前原生文件选择器中手动完成一次文件选择，或提供可用的受控文件选择方式后，再继续一次上传预审流程；仍然禁止最终发布。完成后补充上传成功/失败、预审状态、点击数和耗时证据，并由独立审查线程复验。
