# PROGRAM-ROLLOUT 观察窗口策略修订（2026-07-21）

## 决策

按用户/产品负责人在 2026-07-21 的明确决定，PG-L 稳定观察窗口由“至少 7 天”调整为“至少 1 小时”。本文件是当前执行策略的 Change Record；历史批次中的 7 天记录保留为当时事实，不改写历史证据。

## 新的有效条件

- `minimum_versions >= 1`
- `minimum_hours >= 1`
- `minimum_bounded_runs >= 20`
- 观察期间 rollback trigger 必须为 0：P0/P1、重复上传、误点最终发布、profile 损坏；
- 产品负责人正式 `PG-L` sign-off 仍是独立条件，不因窗口缩短自动完成；
- Windows、真实平台双向 rollback、原生 WebView/生产环境 SLA 仍分别保留其原有边界；
- 不修改已登记的 `window_started_at`，不通过时间戳快进；以当前时间计算实际经过小时数。

## 当前执行口径

观察起点继续使用 `2026-07-21T10:57:15.650469Z`。截至本次执行时已超过 1 小时；主线程将补做一次本地隔离、用户式 UI 操作和 durable run 状态回读，再由独立审查线程复核后更新 PG-L 审计。默认发布 V2、抖音灰度和真实外部平台动作保持关闭/暂停。
