name: Windows Desktop Build

on:
  push:
    branches: [dev, main, "codex/**"]
    paths:
      - ".github/workflows/windows-desktop-build.yml"
      - "api/**"
      - "desktop/**"
      - "pixelle_video/**"
      - "pyproject.toml"
      - "uv.lock"
      - "templates/**"
      - "workflows/**"
      - "docs/contracts/**"
      - "scripts/**"
      - "tests/windows_*_test.py"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/windows-desktop-build.yml"
      - "api/**"
      - "desktop/**"
      - "pixelle_video/**"
      - "pyproject.toml"
      - "uv.lock"
      - "templates/**"
      - "workflows/**"
      - "docs/contracts/**"
      - "scripts/**"
      - "tests/windows_*_test.py"
  workflow_dispatch:
    inputs:
      run_frontend_tests:
        description: "Run the desktop Vitest suite before packaging"
        required: false
        default: true
        type: boolean

permissions:
  contents: read

jobs:
  windows-desktop-build:
    name: Windows NSIS installer and sidecar
    runs-on: windows-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: pwsh
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install locked Python dependencies
        run: uv sync --locked --extra desktop --extra dev

      - name: Build Windows Python sidecar
        run: uv run python desktop/scripts/build_sidecar.py

      - name: Verify Windows sidecar target
        run: |
          $sidecar = "desktop/src-tauri/bin/pixelle-api-x86_64-pc-windows-msvc.exe"
          if (-not (Test-Path -LiteralPath $sidecar)) {
            throw "Windows sidecar was not produced: $sidecar"
          }
          if ((Get-Item -LiteralPath $sidecar).Length -le 0) {
            throw "Windows sidecar is empty: $sidecar"
          }

      - name: Diagnose packaged Windows sidecar
        continue-on-error: true
        run: |
          pwsh -NoProfile -File scripts/windows_sidecar_smoke.ps1 `
            -Sidecar "desktop/src-tauri/bin/pixelle-api-x86_64-pc-windows-msvc.exe" `
            -Output windows-sidecar-smoke.json

      - name: Upload Windows sidecar smoke evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: pixelle-video-windows-sidecar-smoke-${{ github.sha }}
          if-no-files-found: warn
          path: windows-sidecar-smoke.json

      - name: Install desktop dependencies
        working-directory: desktop
        run: npm ci

      - name: Run desktop tests
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.run_frontend_tests }}
        working-directory: desktop
        run: npm run test -- --run

      - name: Build Windows NSIS installer
        working-directory: desktop
        run: npm run tauri build -- --target x86_64-pc-windows-msvc

      - name: Generate artifact manifest
        run: |
          $bundleDir = "desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis"
          $installer = Get-ChildItem -LiteralPath $bundleDir -Filter "*.exe" -File | Select-Object -First 1
          if ($null -eq $installer) {
            throw "No Windows NSIS installer found under $bundleDir"
          }
          uv run python scripts/windows_desktop_artifact_check.py `
            --installer $installer.FullName `
            --sidecar "desktop/src-tauri/bin/pixelle-api-x86_64-pc-windows-msvc.exe" `
            --output windows-artifact-manifest.json

      - name: Install and smoke-test Windows package
        run: |
          $bundleDir = "desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis"
          $installer = Get-ChildItem -LiteralPath $bundleDir -Filter "*.exe" -File | Select-Object -First 1
          if ($null -eq $installer) {
            throw "No Windows NSIS installer found for smoke test"
          }
          pwsh -NoProfile -File scripts/windows_installer_smoke.ps1 `
            -Installer $installer.FullName `
            -Output windows-installer-smoke.json

      - name: Upload Windows installer smoke evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: pixelle-video-windows-installer-smoke-${{ github.sha }}
          if-no-files-found: warn
          path: windows-installer-smoke.json

      - name: Print Windows smoke summaries
        if: ${{ always() }}
        run: |
          if (Test-Path -LiteralPath windows-sidecar-smoke.json) {
            Get-Content -LiteralPath windows-sidecar-smoke.json
          }
          if (Test-Path -LiteralPath windows-installer-smoke.json) {
            Get-Content -LiteralPath windows-installer-smoke.json
          }

      - name: Upload Windows installer artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: pixelle-video-windows-${{ github.sha }}
          if-no-files-found: warn
          path: |
            desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
            desktop/src-tauri/bin/pixelle-api-x86_64-pc-windows-msvc.exe
            windows-artifact-manifest.json
            windows-installer-smoke.json
